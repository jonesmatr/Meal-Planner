<div class="text-center">
  <h2>{{name}}</h2>
  <p>{{{get_emoji}}}</p>
  <p>{{description}}</p>
  <p>Created by {{user.name}} on {{format_date date_created}}</p>
  <p>
    <span for="img" aria-label="calories"></span>
    <span class="Calories Needed">{{format_amount needed_funding}}</span>
    Calories Needed
  </p>
  <p>
    <button class="btn btn-primary">Find Your Meal</button>
  </p>
</div>

